## python类型注解

参考：[Python 类型注解（Type Hints） | 菜鸟教程](https://www.runoob.com/python3/python-type-hints.html)

类型注解就是在代码中注明数据类型的语法，它的核心目的是：

- **提高代码可读性**：让他人（以及未来的你）一眼就能看懂代码的意图
- **便于静态检查**：在运行代码前，通过工具发现潜在的类型错误
- **增强IDE支持**：让代码编辑器提供更准确的自动补全和提示

类型注解无强制校验：对于 

```python
def removeElement(self, nums: List[int], val: int) -> int:
```

即使传入非 `int` 列表（如 `nums = ["1", 2]`），Python 运行时也不会报错（类型注解仅为提示）。

python3.5开始引入的新特性。

## //运算符和int()强制转换

`//` 是**整数除法运算符（Floor Division，也译作 “地板除法”）**，核心作用是执行除法后对结果「向下取整」（向负无穷方向取整），而非简单截断小数部分，这是它与普通除法 `/` 的核心区别。

，`int()` 对浮点数的强制转换**并非 “向下取整”**，而是「向零取整（Truncation towards zero）」—— 即直接截断小数部分，只保留整数部分。

```python
# 正数场景：结果一致
print(int(3.9))   # 3（截断小数）
print(3.9 // 1)   # 3.0（向下取整）

print(int(5.1))   # 5
print(5.1 // 1)   # 5.0

# 负数场景：结果完全不同（关键区别）
print(int(-3.9))  # -3（截断小数，向零取整）
print(-3.9 // 1)  # -4.0（向下取整，向负无穷）

print(int(-5.1))  # -5
print(-5.1 // 1)  # -6.0
```

